import { redirect } from "react-router";
import { url, status } from "../__generated__/[upgrade-history-datahttps:].[clip-hold.taikun.design].[jp].[docs].[upg]._index";

export const loader = () => {
  throw redirect(url, status);
};
